server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customer_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: customer-service

logging:
  level:
    org.springframework.web: DEBUG
